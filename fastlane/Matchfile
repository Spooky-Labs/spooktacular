git_url("https://github.com/Spooky-Labs/spooktacular-certificates")
storage_mode("git")
type("appstore")

# The host app is the only App Store-signed bundle. (The former
# bundled `.systemextension` — com.spooktacular.app.NetworkFilter —
# was removed in the descope, so it no longer needs a profile.)
app_identifier(["com.spooktacular.app"])
