# Swift
.build/
.swiftpm/
*.xcodeproj/xcuserdata/
DerivedData/

# Generated by `xcodegen generate` from `project.yml`.
# Exists only to host the XCUITest bundle that SwiftPM can't
# declare. Never edit directly — edit `project.yml` instead.
Spooktacular.xcodeproj/

# App bundle (built artifact)
Spooktacular.app/
*.app.zip

# Icon build artifacts
Resources/AppIcon.iconset/
Resources/icon_1024.png

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/
# CI-only: `:get_api_key` decodes APPLE_API_KEY_P8 here at
# fastlane-run time and deletes on lane exit. Never commit.
fastlane/ci/

# macOS
.DS_Store
*.swp
*~

# Claude Code
.claude/

# Ruby / Bundler
vendor/bundle/

# Credentials (never commit)
*.p8
*.p12
*.cer
*.mobileprovision

# Fastlane local env (secrets)
fastlane/.env
fastlane/.env.local

# Installer package (built artifact)
*.pkg
